# 开发环境配置示例
# 复制此文件为 .env 并根据需要修改

# =============================================================================
# 基础配置
# =============================================================================

# 调试模式（开发环境建议设为True）
DEBUG=True

# 密钥（开发环境可以使用默认值）
SECRET_KEY=django-insecure-k-dd@oq$uo==9@dr#(fo&+&bt&+l3(m2^(ajhrc*n37_)kmmas

# 允许的主机（开发环境）
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,::1

# =============================================================================
# CSRF配置
# =============================================================================

# CSRF信任的源（开发环境）
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://0.0.0.0:8000,http::1:8000

# CSRF Cookie设置（开发环境不安全）
CSRF_COOKIE_SECURE=False
CSRF_COOKIE_HTTPONLY=False
CSRF_COOKIE_SAMESITE=Lax

# =============================================================================
# 会话配置
# =============================================================================

# 会话Cookie设置（开发环境不安全）
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# =============================================================================
# 邮件配置
# =============================================================================

# 邮件后端（开发环境使用控制台）
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=Meow Site Dev <dev@meowsite.cn>

# =============================================================================
# 静态文件配置
# =============================================================================

# 静态文件URL
STATIC_URL=/static/
MEDIA_URL=/media/

# 静态文件路径（开发环境使用项目目录）
STATIC_ROOT=staticfiles
MEDIA_ROOT=media
STATICFILES_DIRS=static

# =============================================================================
# 日志配置
# =============================================================================

# 日志目录（统一使用项目目录下的logs）
LOG_DIR=logs

# 日志级别（开发环境显示详细信息）
LOG_LEVEL=DEBUG

# =============================================================================
# 数据库配置（SQLite - 开发环境默认）
# =============================================================================

# SQLite是Django默认数据库，无需额外配置
# 如果需要使用MySQL开发环境，可以取消注释以下配置：

# DB_ENGINE=django.db.backends.mysql
# DB_NAME=meowsite_dev
# DB_USER=dev_user
# DB_PASSWORD=dev_password
# DB_HOST=localhost
# DB_PORT=3306